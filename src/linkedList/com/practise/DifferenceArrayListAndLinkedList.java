package linkedList.com.practise;

public class DifferenceArrayListAndLinkedList {
	public static void main(String[] args) {
		
		// arrayList uses dynamic array and linked list uses doubly linked list
		//faster access in arraylist compared to linked list
		//linkedList is good for changing data
		//arraylist is just used for not changing data and just to read data
		// arrayList uses less because it only stores elements
		//but Linked List uses more memory due to storing pointers 
		
		
	}

}
